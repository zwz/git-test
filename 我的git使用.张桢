开始因为看到了版本控制，而git是我看到的地一个版本控制。
所以我就选择学习了一下git。当人那个对我来说还是太难了，首先我发现我没有地方使用git。
事实上，现在也几乎没有。
我对git的理解几乎都是基于对于linux命行行的使用的。
所以我觉得在使用git之前，最好先了解一下linux／unix下面的命令行是怎么回事。
接下来就是得好好看看help文档了，因为在开始的时候实在很难记住命令。
不过常用的几个也挺容易的。
如果是需要别人的项目代码，那就是git clone
建立远程仓库，git remote
更新本地的代码，git pull （就是吧远程仓库的代码弄到本地来）
git push （把本地代码提交到远程仓库）
好像这些《progit》这本书上都有。
我放进来了。

